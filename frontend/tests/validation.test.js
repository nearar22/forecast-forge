import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const app=fs.readFileSync(new URL('../src/main.jsx',import.meta.url),'utf8');const css=fs.readFileSync(new URL('../src/styles.css',import.meta.url),'utf8');test('shows evidence lifecycle',()=>{for(const x of ['SNAPSHOT','CONSENSUS','APPEAL','Portable accuracy'])assert.match(app,new RegExp(x,'i'))});test('has terminal motion and accessibility',()=>{assert.match(css,/@keyframes pulse/);assert.match(css,/@keyframes sweep/);assert.match(css,/prefers-reduced-motion/)})
