import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const app=fs.readFileSync(new URL('../src/main.jsx',import.meta.url),'utf8');const css=fs.readFileSync(new URL('../src/styles.css',import.meta.url),'utf8');test('uses live contract workflow',()=>{for(const x of ['readContract','writeContract','list_markets','commit_forecast','reveal_forecast','submit_evidence','appeal_resolution','finalize_market'])assert.match(app,new RegExp(x))});test('contains no simulated market records',()=>{assert.doesNotMatch(app,/regional grid|open transit|FF-240|demo forecaster/i)});test('keeps terminal motion accessible',()=>{assert.match(css,/@keyframes pulse/);assert.match(css,/prefers-reduced-motion/)})
